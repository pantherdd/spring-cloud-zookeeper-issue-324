server:
  port: 8065
spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    zookeeper:
      connect-string: 'localhost:2181'
      discovery:
        root: /discovery
